# prof-scalper-bot

This repository contains the Next.js dashboard for Prof Scalper Bot.

To start the app:

```bash
cd dashboard
npm install
npm run dev
```

Run the tests:

```bash
cd dashboard
npm test
```
